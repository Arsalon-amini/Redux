let numbers = [1, 2, 3];
numbers.map((number) => number * 2); //map is a higher order fn -> it takes a fn as an argument

setTimeout(() => console.log("Hello"), 1000); //takes a fn as arg -> higher order fn
